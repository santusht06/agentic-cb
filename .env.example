# Agentic CLI Browser Configuration (Optional)

# CDP Port for native Chrome attachment (default: 9222)
CDP_PORT=9222

# Default profile name for persistent sessions (default: default)
DEFAULT_PROFILE=default

# Headless mode: true or false (default: true)
HEADLESS=true
